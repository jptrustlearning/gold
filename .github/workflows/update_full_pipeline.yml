name: Daily Full Pipeline (Market Data → Gold Price → Momentum Score)

on:
  schedule:
    # รัน 4 รอบ ห่างกัน 1 ชม. — ป้องกัน data ยังไม่มา
    # 23:30, 00:30, 01:30, 02:30 UTC = 06:30, 07:30, 08:30, 09:30 ไทย
    # ใช้นาที :30 เลี่ยง conflict กับ H1 workflow ที่รันนาที :05
    - cron: '30 23 * * *'
    - cron: '30 0 * * *'
    - cron: '30 1 * * *'
    - cron: '30 2 * * *'
  workflow_dispatch: # รัน manual ได้จาก Actions tab

jobs:
  full-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GOLD_PAT }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance pandas numpy openpyxl

      # ════════════════════════════════════════════════════════════
      # STEP 1: ดึง Market Data (DXY, US10Y, Brent, WTI, VIX)
      # ════════════════════════════════════════════════════════════
      - name: "Step 1: Fetch market data (DXY, US10Y, Brent, WTI, VIX)"
        run: |
          python3 << 'PYEOF'
          import yfinance as yf
          import pandas as pd
          from datetime import datetime, timedelta
          import os, time

          FALLBACK_START = '2020-01-01'

          ASSETS = [
              {"symbol": "DX-Y.NYB", "csv": "dxy_prices.csv",   "name": "DXY",          "decimals": 2},
              {"symbol": "^TNX",     "csv": "us10y_prices.csv", "name": "US 10Y Yield", "decimals": 3},
              {"symbol": "BZ=F",     "csv": "brent_prices.csv", "name": "Brent Crude",  "decimals": 2},
              {"symbol": "CL=F",     "csv": "wti_prices.csv",   "name": "WTI Crude",    "decimals": 2},
              {"symbol": "^VIX",     "csv": "vix_prices.csv",   "name": "VIX",          "decimals": 2},
          ]

          results = []

          for asset in ASSETS:
              symbol = asset["symbol"]
              csv_file = asset["csv"]
              name = asset["name"]
              decimals = asset["decimals"]

              print(f'\n{"="*50}')
              print(f'📊 {name} ({symbol})')
              print(f'{"="*50}')

              if os.path.exists(csv_file):
                  df_existing = pd.read_csv(csv_file, encoding='utf-8-sig')
                  df_existing.columns = ['Date','Open','High','Low','Close','Volume']
                  df_existing['Date'] = pd.to_datetime(df_existing['Date'])
                  last_date = df_existing['Date'].max()
                  start_date = (last_date + timedelta(days=1)).strftime('%Y-%m-%d')
                  print(f'📂 Existing: {len(df_existing)} rows, last: {last_date.date()}')
              else:
                  df_existing = pd.DataFrame(columns=['Date','Open','High','Low','Close','Volume'])
                  start_date = FALLBACK_START
                  print(f'📂 No existing file — starting from {FALLBACK_START}')

              end_date = datetime.now().strftime('%Y-%m-%d')
              print(f'🔄 Fetching: {start_date} → {end_date}')

              df_new = pd.DataFrame()
              for attempt in range(3):
                  try:
                      ticker = yf.Ticker(symbol)
                      df_new = ticker.history(start=start_date, end=end_date)
                      break
                  except Exception as e:
                      print(f'⚠️  Attempt {attempt+1}/3 failed for {symbol}: {e}')
                      if attempt < 2:
                          wait = 30 * (attempt + 1)
                          print(f'⏳ Waiting {wait}s before retry...')
                          time.sleep(wait)
                      else:
                          print(f'❌ All retries failed for {symbol}')
                          results.append({"name": name, "status": "error", "new": 0})
              
              if df_new is None or (hasattr(df_new, 'empty') and df_new.empty and len(results) > 0 and results[-1].get("status") == "error"):
                  continue

              if df_new.empty:
                  print(f'ℹ️  No new data available')
                  results.append({"name": name, "status": "skip", "new": 0})
                  continue

              try:
                  df_new = df_new[['Open','High','Low','Close','Volume']].reset_index()
                  df_new.rename(columns={'Date': 'Date_raw'}, inplace=True)
                  if df_new['Date_raw'].dt.tz is not None:
                      df_new['Date_raw'] = df_new['Date_raw'].dt.tz_localize(None)
                  df_new['Date'] = df_new['Date_raw']
                  df_new = df_new[['Date','Open','High','Low','Close','Volume']]
                  for col in ['Open','High','Low','Close']:
                      df_new[col] = df_new[col].round(decimals)
                  df_new['Volume'] = df_new['Volume'].fillna(0).astype(int)
                  new_count = len(df_new)

                  df_all = pd.concat([df_existing, df_new], ignore_index=True)
                  df_all['Date'] = pd.to_datetime(df_all['Date'])
                  df_all = df_all[df_all['Date'].dt.dayofweek < 5]  # Remove weekend rows
                  df_all = df_all.drop_duplicates(subset='Date', keep='last')
                  df_all = df_all.sort_values('Date').reset_index(drop=True)

                  df_out = df_all.copy()
                  df_out['Date'] = df_out['Date'].dt.strftime('%Y-%m-%d')
                  df_out.columns = ['วันที่','ราคาเปิด','ราคาสูงสุด','ราคาต่ำสุด','ราคาปิด','ปริมาณซื้อขาย']
                  df_out.to_csv(csv_file, index=False, encoding='utf-8-sig')

                  latest = df_all['Close'].iloc[-1]
                  print(f'✅ Total: {len(df_all)} rows (+{new_count} new) | Latest: {latest:.{decimals}f}')
                  results.append({"name": name, "status": "ok", "new": new_count, "csv": csv_file})
              except Exception as e:
                  print(f'❌ Error processing {name}: {e}')
                  results.append({"name": name, "status": "error", "new": 0})

              # Delay between assets to avoid rate limit
              time.sleep(5)

          # Summary
          print(f'\n{"="*50}')
          print('📋 MARKET DATA SUMMARY')
          print(f'{"="*50}')
          market_parts = []
          for r in results:
              if r["status"] == "ok":
                  print(f'  ✅ {r["name"]}: +{r["new"]} rows')
                  market_parts.append(f'{r["name"]}+{r["new"]}')
              elif r["status"] == "skip":
                  print(f'  ⏭️  {r["name"]}: no new data')
              else:
                  print(f'  ❌ {r["name"]}: error')

          with open('MARKET_SUMMARY', 'w') as f:
              f.write(' | '.join(market_parts) if market_parts else 'no new data')
          PYEOF

      # ════════════════════════════════════════════════════════════
      # STEP 2: ดึงราคาทอง + อัพเดท gold_prices.csv
      # ════════════════════════════════════════════════════════════
      - name: "Step 2: Fetch gold prices"
        run: |
          python3 << 'EOF'
          import yfinance as yf
          import pandas as pd
          from datetime import datetime, timedelta
          import os, time

          SYMBOL = 'GC=F'
          CSV_FILE = 'gold_prices.csv'
          FALLBACK_START = '2020-01-01'

          if os.path.exists(CSV_FILE):
              df_existing = pd.read_csv(CSV_FILE, encoding='utf-8-sig')
              df_existing.columns = ['Date','Open','High','Low','Close','Volume']
              df_existing['Date'] = pd.to_datetime(df_existing['Date'])
              last_date = df_existing['Date'].max()
              start_date = (last_date + timedelta(days=1)).strftime('%Y-%m-%d')
              print(f'📂 Existing gold: {len(df_existing)} rows, last: {last_date.date()}')
          else:
              df_existing = pd.DataFrame(columns=['Date','Open','High','Low','Close','Volume'])
              start_date = FALLBACK_START

          end_date = datetime.now().strftime('%Y-%m-%d')
          print(f'🔄 Fetching {SYMBOL}: {start_date} → {end_date}')

          # Wait before fetching to avoid rate limit from Step 1
          print('⏳ Waiting 30s to avoid Yahoo Finance rate limit...')
          time.sleep(30)

          df_new = pd.DataFrame()
          for attempt in range(3):
              try:
                  gold = yf.Ticker(SYMBOL)
                  df_new = gold.history(start=start_date, end=end_date)
                  break
              except Exception as e:
                  print(f'⚠️  Attempt {attempt+1}/3 failed: {e}')
                  if attempt < 2:
                      wait = 30 * (attempt + 1)
                      print(f'⏳ Waiting {wait}s before retry...')
                      time.sleep(wait)
                  else:
                      print(f'❌ All retries failed for {SYMBOL}')
                      raise

          if df_new.empty:
              print('ℹ️  No new gold data')
              with open('GOLD_INFO', 'w') as f:
                  f.write('no new data')
          else:
              df_new = df_new[['Open','High','Low','Close','Volume']].reset_index()
              df_new.rename(columns={'Date': 'Date_raw'}, inplace=True)
              if df_new['Date_raw'].dt.tz is not None:
                  df_new['Date_raw'] = df_new['Date_raw'].dt.tz_localize(None)
              df_new['Date'] = df_new['Date_raw']
              df_new = df_new[['Date','Open','High','Low','Close','Volume']]
              for col in ['Open','High','Low','Close']:
                  df_new[col] = df_new[col].round(1)
              df_new['Volume'] = df_new['Volume'].astype(int)
              new_count = len(df_new)

              df_all = pd.concat([df_existing, df_new], ignore_index=True)
              df_all['Date'] = pd.to_datetime(df_all['Date'])
              df_all = df_all.drop_duplicates(subset='Date', keep='last')
              df_all = df_all.sort_values('Date').reset_index(drop=True)

              df_out = df_all.copy()
              df_out['Date'] = df_out['Date'].dt.strftime('%Y-%m-%d')
              df_out.columns = ['วันที่','ราคาเปิด','ราคาสูงสุด','ราคาต่ำสุด','ราคาปิด','ปริมาณซื้อขาย']
              df_out.to_csv(CSV_FILE, index=False, encoding='utf-8-sig')

              latest_price = df_all['Close'].iloc[-1]
              latest_date = df_all['Date'].max().strftime('%Y-%m-%d')
              print(f'✅ Gold: {len(df_all)} rows (+{new_count}) | ${latest_price:.1f} @ {latest_date}')

              with open('GOLD_INFO', 'w') as f:
                  f.write(f'+{new_count} rows | ${latest_price:.1f} @ {latest_date}')
          EOF

      # ════════════════════════════════════════════════════════════
      # STEP 3: รัน Momentum Scoring V3 — BUY + SELL
      # ════════════════════════════════════════════════════════════
      - name: "Step 3a: Run Momentum Scoring V3 — BUY side"
        run: |
          echo "🎯 Running Gold Momentum Score v3.0 (BUY)..."
          python3 gold_momentum_v2.py
          echo "✅ Buy scoring complete"

      - name: "Step 3b: Run Momentum Scoring V3 — SELL side"
        run: |
          echo "🎯 Running Gold Momentum Score v3.0 (SELL)..."
          python3 gold_momentum_sell.py
          echo "✅ Sell scoring complete"

      - name: "Step 3c: Calculate Net Bias (BUY vs SELL)"
        run: |
          echo "🎯 Calculating Net Bias..."
          python3 gold_momentum_net.py
          echo "✅ Net Bias complete"

      # ════════════════════════════════════════════════════════════
      # STEP 3H: H1 Timeframe Pipeline (BUY + SELL + NET)
      # ════════════════════════════════════════════════════════════
      - name: "Step 3h-a: Run H1 Momentum Scoring — BUY side"
        run: |
          echo "📊 Running H1 Buy Momentum..."
          python3 gold_momentum_h1.py
          echo "✅ H1 Buy complete"

      - name: "Step 3h-b: Run H1 Momentum Scoring — SELL side"
        run: |
          echo "📊 Running H1 Sell Momentum..."
          python3 gold_momentum_h1_sell.py
          echo "✅ H1 Sell complete"

      - name: "Step 3h-c: Calculate H1 Net Bias (BUY vs SELL)"
        run: |
          echo "🎯 Calculating H1 Net Bias..."
          python3 gold_momentum_h1_net.py
          echo "✅ H1 Net Bias complete"

      # ════════════════════════════════════════════════════════════
      # STEP 4: Commit & Push ทุกอย่างพร้อมกัน
      # ════════════════════════════════════════════════════════════
      - name: "Step 4: Commit and push all outputs"
        run: |
          git config user.email "jptrustlearning@users.noreply.github.com"
          git config user.name "JP Trust Learning"

          TIMESTAMP=$(date -u +"%d/%m/%Y %H:%M UTC")
          MARKET_SUMMARY=$(cat MARKET_SUMMARY 2>/dev/null || echo "")
          GOLD_INFO=$(cat GOLD_INFO 2>/dev/null || echo "")

          # Add market data CSVs
          git add dxy_prices.csv us10y_prices.csv brent_prices.csv wti_prices.csv vix_prices.csv 2>/dev/null || true

          # Add gold prices
          git add gold_prices.csv 2>/dev/null || true

          # Add momentum outputs (BUY + SELL + NET)
          git add output_momentum_gold.csv 2>/dev/null || true
          git add output_momentum_gold_*.csv 2>/dev/null || true
          git add output_momentum_gold_sell.csv 2>/dev/null || true
          git add output_momentum_gold_sell_*.csv 2>/dev/null || true
          git add output_momentum_gold_net.csv 2>/dev/null || true
          git add output_momentum_gold_net_*.csv 2>/dev/null || true
          git add score_history.csv 2>/dev/null || true
          git add score_history_sell.csv 2>/dev/null || true

          # H1 outputs
          git add gold_prices_h1_tail.csv 2>/dev/null || true
          git add output_momentum_gold_h1.csv 2>/dev/null || true
          git add output_momentum_gold_h1_*.csv 2>/dev/null || true
          git add output_momentum_gold_h1_sell.csv 2>/dev/null || true
          git add output_momentum_gold_h1_sell_*.csv 2>/dev/null || true
          git add output_momentum_gold_h1_net.csv 2>/dev/null || true
          git add output_momentum_gold_h1_net_*.csv 2>/dev/null || true
          git add score_history_h1.csv 2>/dev/null || true
          git add score_history_h1_sell.csv 2>/dev/null || true

          # ไม่ push Excel (ใหญ่เกินไป)
          git reset HEAD Gold_Momentum_v3.0.xlsx 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "✅ No changes to push"
          else
            # Commit message รวมทุก step
            MSG="🔄 Full Pipeline: Market(${MARKET_SUMMARY}) | Gold(${GOLD_INFO}) | Buy+Sell v3.0 — ${TIMESTAMP}"
            git commit -m "$MSG"
            git push
            echo "✅ Pushed to GitHub"
            echo "📝 $MSG"
          fi

          # Cleanup
          rm -f MARKET_SUMMARY GOLD_INFO NO_NEW_DATA
